<h2><%= t 'devise.views.unblock.new.title' %></h2>

<%= simple_form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :email, :label => t('devise.attributes.email'), :required => true %>
  </div>

  <div class="actions">
    <%= f.button :submit, t('devise.views.unblock.new.submit') %>
  </div>
<% end %>

<%= render :partial => "devise/shared/links" %>
